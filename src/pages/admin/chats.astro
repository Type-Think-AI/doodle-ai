---
// Server-rendered, never static — see src/pages/admin/index.astro for why
// this line is load-bearing rather than a formality.
export const prerender = false;

import AdminLayout from '../../admin/layouts/AdminLayout.astro';
import ChatsPage from '../../admin/pages/ChatsPage.astro';

const admin = Astro.locals.admin!;
---

<AdminLayout active="chats" admin={admin}>
	<ChatsPage />
</AdminLayout>
