# LANES — clear `pnpm lint` (Aug 30 2026)

## Starting state

`pnpm lint` reported **56 errors**. I already fixed **21** of them with one
config block: every `no-undef` was `chrome` in `tools/webmcp-agent-tester/**`,
which is a Chrome extension whose scripts run against the `chrome.*` APIs.
ESLint had `globals.browser` + `globals.node` but never `globals.webextensions`,
so a config gap was being reported as 21 source bugs.

**Do not re-fix those.** `eslint.config.mjs` is RESERVED.

**35 errors remain**, split into the three lanes below. `pnpm build` is currently
green — keep it that way.

## Rules for every lane

- Fix the underlying problem. Do **not** silence a rule with an inline
  `eslint-disable` unless your lane explicitly authorises it below — the default
  is a real fix.
- `preserve-caught-error`: attach the original error, e.g.
  `throw new Error("msg", { cause: err })`. Do not just rename the variable.
- `no-unused-vars` on a caught error: if the error genuinely is not needed, use
  the bare `catch {` form rather than `catch (e)` with an unused binding.
- `no-useless-assignment`: the value is written and never read before being
  overwritten — delete the dead write, don't reorder logic you don't understand.
- Verify with `pnpm exec eslint <your files>` and confirm **zero** errors in your
  lane. Then confirm you did not break anything: these are scripts and an
  extension, so read the surrounding function before deleting a variable.
- Do NOT spawn sub-agents. Do NOT commit or push.

## RESERVED — only the main agent edits these

- `eslint.config.mjs`
- `package.json`, `pnpm-lock.yaml`
- `src/components/agentic/tools-articles.ts`
- `src/lib/admin/media.ts`
- anything not named in your own lane

## Lane A — the Chrome extension (9 errors)

**You own:**
- `tools/webmcp-agent-tester/sidepanel.js` (7)
- `tools/webmcp-agent-tester/content.js` (1)
- `tools/webmcp-agent-tester/injected.js` (1)

All remaining errors here are `no-unused-vars`, `no-useless-assignment`,
`preserve-caught-error` and `no-empty`. The `chrome` globals are already handled
— if you still see a `chrome` `no-undef`, stop and report it rather than editing.

`no-empty` on a `catch {}`: an intentionally-ignored failure needs a one-line
comment saying *why* it is safe to ignore, which also satisfies the rule. Do not
add a no-op statement.

This extension is a real debugging tool (it drives WebMCP tool calls in a live
page). Read enough of each function to be sure a variable is genuinely dead
before removing it — `signalAbort` and `dt` in particular look like they were
meant to be used, so say so in your report if the dead value hints at a bug.

## Lane B — the WebMCP scripts (11 errors)

**You own:**
- `scripts/webmcp-diag.mjs` (3)
- `scripts/webmcp-agent-eval.mjs` (2)
- `scripts/webmcp-smoke.mjs` (2)
- `scripts/webmcp-spec-watch.mjs` (2)
- `scripts/webmcp-tools-test.mjs` (2)

Mostly `no-unused-vars`, `@typescript-eslint/no-unused-expressions` and
`preserve-caught-error`.

`no-unused-expressions` usually means a statement that computes something and
discards it — often an `a && b()` used as a statement, or a stray expression left
from a refactor. Convert to a real `if`, or delete it if it is dead. Do not wrap
it in `void` to silence the rule unless the expression is a deliberate
fire-and-forget promise, in which case `void promise` IS correct and you should
say so.

These scripts are the WebMCP conformance harness and are run in CI-ish fashion
(`pnpm test:webmcp`). Do not change their behaviour or their exit codes.

## Lane C — article-sections.ts (13 errors)

**You own exactly `src/lib/content/article-sections.ts`.**

Read the file first. **12 of the 13 errors are on line 44**, which is an
8,173-character regex with this comment above it:

> The exact character class github-slugger 2.0.0 strips before hyphenating.
> Copied verbatim from `github-slugger/regex.js` (generated by its own
> `script/`). Do not edit by hand; refresh from the package if it changes.

**DO NOT rewrite that regex.** It is a verbatim vendored copy, and it decides
every heading anchor id on the site. Changing it would silently change every
`#section` anchor — breaking external links and the WebMCP section-slug API — and
would destroy the one property that makes it auditable: being byte-identical to
upstream.

For line 44 only, this lane **authorises a scoped `eslint-disable`**, placed
immediately above the declaration, listing exactly the three rules involved
(`no-control-regex`, `no-useless-escape`, `no-misleading-character-class`) and
stating in a comment WHY: it is generated third-party source, the escapes and
combined characters are upstream's, and hand-editing it would change site-wide
anchor ids. Do not use a file-wide disable and do not disable any other rule.

**Line 103 is a genuine bug — fix it properly.** In
`.replace(/\\([\\`*_{}\[\]()#+\-.!~>|])/g, "$1")` the `\[` inside the character
class is an unnecessary escape. Fix the escape without changing which characters
the class matches — that replace strips markdown escaping, so getting it wrong
would corrupt article text. State how you verified the class is unchanged.

Report which errors you fixed properly versus the one declaration you disabled,
and note that I am separately considering adding `github-slugger@2.0.0` as a
direct dependency so the copy can be deleted entirely — do NOT do that yourself
(`package.json` is reserved).
